/**
 *          -- Lazy Initialization --
 * In lazy initialization the value is only initialized if any part of the program requests it.
 *      - This removes the unused initialization that might take up memory space for no reason
 *
 *
 * */